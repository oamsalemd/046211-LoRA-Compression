Code:
* Add comments to the code
* Delete unused functions/code sections
* Modify full path directories to relative paths/arguments

README:
* Environment setup
* How to run the code
    -> Optuna trials w/ SVD (static function/file, callable from CMD line)
    -> Optuna trials w/o SVD (static function/file, callable from CMD line)
* Project goal & Method - copy from slides
* Compression benefits (w/ formula)

Github:
* Plots for training results
* Saved models per each Optuna study
* Additional required environment files (e.g. requirements.txt)

Presentation:
* Results graphs
* Compression benefits (w/ formula)